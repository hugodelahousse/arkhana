{
  "eval_id": 2,
  "eval_name": "migration",
  "prompt": "We have a Tailwind v4 project with tokens like --color-bg-primary (dark page bg), --color-bg-secondary (card bg), --color-text-default (main text), --color-text-muted (dimmer text), --color-border-ui (dividers). Components use classes like bg-bg-primary, text-text-default, border-border-ui. Help me migrate to shadcn-style naming conventions so the token names are more standard and AI coding tools generate correct code.",
  "assertions": [
    "Provides a mapping of old names to new shadcn names (--background, --foreground, --card, --muted, --muted-foreground, --border)",
    "Produces or describes a new tokens.css with the shadcn semantic :root block",
    "Provides the @theme inline block mapping --color-background, --color-foreground, etc. from the semantic vars",
    "Provides concrete guidance on renaming utility classes in components (sed commands or find-and-replace patterns)",
    "Warns about the double-transformation bug: word-boundary sed patterns can corrupt CSS var names in inline styles"
  ]
}
